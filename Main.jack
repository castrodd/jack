/** Initializes a new Jack-pardy! game and starts running it. */
class Main {
    function void main() {
        var JackpardyGame game;
        let game = JackpardyGame.new();
        do game.run();
        do game.dispose();
        return;
    }
}